"""Legacy models module kept for backwards compatibility.

The canonical models now live in app/models/*.py. This file is intentionally
left minimal to avoid conflicting duplicate declarations (e.g., the User
model). Add new shared model glue here only if absolutely required.
"""

# Intentionally empty to prevent duplicate model definitions.

